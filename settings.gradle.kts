rootProject.name = "creditSimulator"

pluginManagement {
    repositories {
        gradlePluginPortal() // 🔥 ESSENCIAL para encontrar plugins Gradle
        mavenCentral()
        google()
    }
}
